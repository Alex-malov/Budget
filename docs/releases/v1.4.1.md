# Budget 1.4.1 — кандидат релиза производительности

Дата: 26.08.2026. База: 1.4.0. Статус: **кандидат на согласование**.

## Изменения

- единый API запуска `GET /api/bootstrap` вместо семи независимых запросов;
- одно построение начислений на срез вместо повторного расчёта для каждого проекта;
- индексы НСИ и фактических часов вместо повторных полных проходов по реестрам;
- параллельное чтение локальных JSON-данных и сохранение существующей бизнес-логики;
- версия приложения: `1.4.1`.

## Подтверждение

- 8/8 автоматических тестов;
- `GET /api/bootstrap`: в среднем 0,11 с по пяти локальным запросам;
- первый финансовый экран: около 0,25 с;
- переходы по основным разделам: около 0,28–0,30 с;
- подробности: [архитектурный анализ производительности](../architecture/performance-v1.4.1.md).

Desktop-сборка и замена установленного кандидата выполняются после отдельного согласования владельцем продукта.
